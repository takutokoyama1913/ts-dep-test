{
  "name": "ts-dep-test",
  "version": "0.1.0",
  "description": "",
  "main": "dist/index.js",
  "module": "dist",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "build:refresh": "npm run clean && npm run build",
    "clean": "rimraf dist/*"
  },
  "dependencies": {
    "@types/ramda": "^0.26.35",
    "ramda": "^0.26.1"
  },
  "devDependencies": {
    "rimraf": "^3.0.0",
    "typescript": "^3.7.2"
  }
}
